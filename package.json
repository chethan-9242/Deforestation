{
  "name": "deforestation-detection",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && chmod +x node_modules/.bin/* && npm run build",
    "start": "cd frontend && npm start"
  }
}
